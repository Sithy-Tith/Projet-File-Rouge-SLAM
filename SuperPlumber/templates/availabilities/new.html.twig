{% extends 'base.html.twig' %}

{% block title %}Ajouter une Disponibilité{% endblock %}

{% block body %}
<div class="m-3">
    <h1>Ajouter une Disponibilité</h1>
</div>

<div class="container-fluid">
    <div class="card col-8 mx-3">
        <div class="m-3">
            {{ include('availabilities/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
        <div class="mx-2">
            <a href="{{ path('app_availabilities_index') }}" class="btn btn-secondary m-2">Annuler</a>
        </div>
    </div>
</div>
{% endblock %}