{% extends 'base.html.twig' %}

{% block title %}Modifier une pièce{% endblock %}

{% block body %}

<div class="m-3">
    <h1>Modifier {{ piece.name }}</h1>
</div>

<div class="container-fluid">
    <div class="card col-8 mx-3">
        <div class="m-3">
            {{ include('pieces/_form.html.twig', {'button_label': 'Enregistrer'}) }}

            <a href="{{ path('app_pieces_index') }}" class="btn btn-primary m-2">Retour à la liste</a>

            {{ include('pieces/_delete_form.html.twig') }}
            {% endblock %}