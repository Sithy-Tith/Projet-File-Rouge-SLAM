<form method="post" action="{{ path('app_used_pieces_delete', {'id': used_piece.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ used_piece.id) }}">
    <button class="btn">Delete</button>
</form>
